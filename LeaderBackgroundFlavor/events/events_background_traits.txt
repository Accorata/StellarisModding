namespace = leader_background
country_event = {
	id = leader_background.0
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		NOT = {
			has_country_flag = spawning_renowned_leader			#maybe?
		}
	}
	immediate = {
		change_background_job = servant
		if = {
			limit = {
				from = {
					has_background_job = servant
				}
			}
		}
		from = {
			add_trait_no_notify = leader_trait_synthetic
		}
	}
}
