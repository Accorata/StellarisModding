if = {
	limit = {
		has_acot = no
	}
	has_upgraded_capital = yes
}
if = {
	limit = {
		has_acot = yes
	}
	OR = {
		has_upgraded_capital = yes
		has_enigmatic_capital = yes
		has_ascended_capital = yes
	}
}