ac_effective_ruler_level = {
	complex_trigger_modifier = {
		trigger = has_total_skill
		trigger_scope = owner.ruler
		mode = add
	}
}

ac_num_assimilation_engine_value = {
	base = 0
	add = ac_num_assimilation_engine
}

ac_hive_virtuality_concentrated_production_mult = {
	base = 0.75
	complex_trigger_modifier = {
		trigger = count_owned_planet
		trigger_scope = owner
		mult = 0.10
		mode = subtract
	}
}

ac_num_cloning_vats = {
	complex_trigger_modifier = {
		trigger = count_owned_planet
		trigger_scope = owner
		parameters = {
			limit = {
				is_colony = yes
				has_active_building = building_clone_vats
			}
		}
		mode = add
	}
	complex_trigger_modifier = {
		trigger = count_owned_planet
		trigger_scope = owner
		parameters = {
			limit = {
				is_colony = yes
				has_active_building = building_clone_army_clone_vat
			}
		}
		mult = 2
		mode = add
	}
}

ac_empire_size_traditions_reduction = {
	base = 1
	add = modifier:empire_size_penalty_mult
	add = modifier:tradition_cost_empire_size_mult
	min = 1
}

ac_empire_size_tradition_effect = {
	add = value:negative_empire_size_percent
	mult = value:ac_empire_size_traditions_reduction
	min = 0.2
	max = 1
}

ac_counter_cloned = {
	complex_trigger_modifier = {
		trigger = count_owned_pop_amount
		parameters = {
			limit = {
				has_trait = trait_ac_cloned
			}
		}
		mode = add
	}
	max = 50000
}

ac_get_weighted_attunement_points = {
	base = 20
	complex_trigger_modifier = {
		trigger = get_attunement_points_for
		parameters = {
			patron = $PATRON$
		}
		mode = add
	}
	divide = 100
	min = $INTERVAL$
	round_to = $INTERVAL$
}

ac_country_double_envoy_chance = {
	add = modifier:ac_double_envoy_chance_add
	mult = 100
}

# Future Usage:
# value:ac_districts_arcology_jobs|TYPE|<district>|FACTOR|2|
ac_districts_arcology_jobs = {
	complex_trigger_modifier = {
		trigger = num_districts
		parameters = {
			type = $TYPE$
		}
		mult = $FACTOR$		# 2
		mode = add
	}
}
