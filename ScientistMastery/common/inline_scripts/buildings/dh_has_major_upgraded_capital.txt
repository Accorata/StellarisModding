if = {
	limit = {
		has_acot = no
	}
	has_major_upgraded_capital = yes
}
if = {
	limit = {
		has_acot = yes
	}
	OR = {
		has_major_upgraded_capital = yes
		has_enigmatic_capital = yes
		has_ascended_capital = yes
	}
}